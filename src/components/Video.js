import React from 'react'

import styled from 'styled-components'
import tw from 'tailwind.macro'

const EmbeddedVideo = ({ videoSrcURL, videoTitle, ...props }) => (
	<div className="">Tjenare</div>
)
